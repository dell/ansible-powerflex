---
- name: SDT Operations
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    # Oslo1
    hostname: "x.x.x.x"
    username: "admin"
    password: "Password123!"
    validate_certs: false
    host_port: 443
    sdt_name: "trinity-sdt1"
    sdt_new_name: "sdt_new"
    protection_domain_name: "trinity-PD"
    sdt_ip1: "x.x.x.x"

  tasks:
    # Skip SDT creation due to communication error with x.x.x.x
    # This requires the SDT software to be installed and network connectivity

    - name: Get SDT details (query operation)
      dellemc.powerflex.sdt:
        hostname: "{{ hostname }}"
        username: "{{ username }}"
        password: "{{ password }}"
        validate_certs: "{{ validate_certs }}"
        sdt_name: "{{ sdt_name }}"
        protection_domain_name: "{{ protection_domain_name }}"
        sdt_ip_list:
          - ip: "{{ sdt_ip1 }}"
            role: "StorageAndHost"
        state: "present"
