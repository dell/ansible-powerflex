---
- name: SDT Operations
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    # Oslo1
    hostname: "10.247.39.38"
    username: "admin"
    password: "Password123!"
    validate_certs: false
    host_port: 443
    sdt_name: "trinity-sdt1"
    sdt_new_name: "sdt_new"
    protection_domain_name: "trinity-PD"
    sdt_ip1: "10.247.58.31"

  tasks:
    # Skip SDT creation due to communication error with 10.247.58.31
    # This requires the SDT software to be installed and network connectivity

    - name: Get SDT details (query operation)
      dellemc.powerflex.sdt:
        hostname: "{{ hostname }}"
        username: "{{ username }}"
        password: "{{ password }}"
        validate_certs: "{{ validate_certs }}"
        sdt_name: "{{ sdt_name }}"
        protection_domain_name: "{{ protection_domain_name }}"
        sdt_ip_list:
          - ip: "{{ sdt_ip1 }}"
            role: "StorageAndHost"
        state: "present"
